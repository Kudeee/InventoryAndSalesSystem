<UserControl x:Class="InventoryAndSalesSystem.Views.AddEditProductView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InventoryAndSalesSystem.Views"
             mc:Ignorable="d" d:DesignWidth="1558" Height="778">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Add / Edit Product" FontSize="18" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBlock Text="Create new products or modify existing ones" 
                      FontSize="10" Foreground="Gray"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Form -->
            <Border Grid.Column="0" Background="White" BorderBrush="#E0E0E0" 
                   BorderThickness="1" CornerRadius="5" Padding="20">
                <StackPanel>
                    <TextBlock Text="Product Details" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>

                    <TextBlock Text="Product Name *" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" 
                            Height="35" Padding="8" Margin="0,0,0,10"/>

                    <TextBlock Text="Category *" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <ComboBox ItemsSource="{Binding Categories}"
                             SelectedItem="{Binding Category}"
                             IsEditable="True"
                             Height="35" Padding="8" Margin="0,0,0,10">
                        <ComboBox.Style>
                            <Style TargetType="ComboBox">
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="">
                                        <Setter Property="Background">
                                            <Setter.Value>
                                                <VisualBrush Stretch="None" AlignmentX="Left">
                                                    <VisualBrush.Visual>
                                                        <TextBlock Text="Select or type category..." 
                                                                  Foreground="#999999" 
                                                                  Margin="10,0,0,0"
                                                                  VerticalAlignment="Center"/>
                                                    </VisualBrush.Visual>
                                                </VisualBrush>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ComboBox.Style>
                    </ComboBox>
                    
                   <TextBlock Text="Unit Cost *" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding UnitCost, UpdateSourceTrigger=PropertyChanged}"
                               Height="35" Padding="8" Margin="0,0,0,10"/>
                    
                    <TextBlock Text="Price *" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding Price, UpdateSourceTrigger=PropertyChanged}" 
                            Height="35" Padding="8" Margin="0,0,0,10"/>

                    <TextBlock Text="Stock Quantity *" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding Stock, UpdateSourceTrigger=PropertyChanged}" 
                            Height="35" Padding="8" Margin="0,0,0,10"/>

                    <TextBlock Text="Minimum Stock Level *" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding MinStock, UpdateSourceTrigger=PropertyChanged}" 
                            Height="35" Padding="8" Margin="0,0,0,10"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button Content="💾 Save Product" 
                               Command="{Binding SaveCommand}"
                               Style="{StaticResource PrimaryButton}"
                               Width="150" Height="40"/>
                        <Button Content="🗑️ Delete" 
                               Command="{Binding DeleteCommand}"
                               Style="{StaticResource DangerButton}"
                               Width="100" Height="40"/>
                        <Button Content="Clear" 
                               Command="{Binding ClearCommand}"
                               Background="#757575"
                               Foreground="White"
                               Width="100" Height="40"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Product List -->
            <Border Grid.Column="2" Background="White" BorderBrush="#E0E0E0" 
                   BorderThickness="1" CornerRadius="5" Padding="15">
                <StackPanel>
                    <TextBlock Text="Select Product to Edit" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    
                        <ListBox ItemsSource="{Binding Products}"
                            SelectedItem="{Binding SelectedProduct}"
                            Height="450">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="5">
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding Category}" 
                                                   FontSize="10" 
                                                   Foreground="#2196F3"
                                                   FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding UnitCost, StringFormat='Unit Cost: {0:C}', ConverterCulture='en-PH'}"
                                                   FontSize="11" Foreground="Gray"/>
                                        <TextBlock Text="{Binding Price, StringFormat='Price: {0:C}', ConverterCulture='en-PH'}" 
                                        FontSize="11" Foreground="Gray"/>
                                        <TextBlock Text="{Binding Stock, StringFormat='Stock: {0}'}" 
                                        FontSize="11" Foreground="Gray"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
