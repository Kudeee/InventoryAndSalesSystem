<UserControl x:Class="InventoryAndSalesSystem.Views.AuditTrailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Audit Trail" FontSize="22" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBlock Text="Complete log of every action performed in the system"
                       FontSize="12" Foreground="Gray"/>
        </StackPanel>

        <!-- Filter Bar -->
        <Border Grid.Row="1" Background="White" BorderBrush="#E0E0E0"
                BorderThickness="1" CornerRadius="6" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="160"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="160"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="🔍 Search" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,3"/>
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             Height="35" Padding="8"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Action Type" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,3"/>
                    <ComboBox ItemsSource="{Binding ActionFilters}"
                              SelectedItem="{Binding SelectedActionFilter}"
                              Height="35" Padding="8"/>
                </StackPanel>

                <StackPanel Grid.Column="4">
                    <TextBlock Text="From Date" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,3"/>
                    <DatePicker SelectedDate="{Binding FromDate}" Height="35"/>
                </StackPanel>

                <StackPanel Grid.Column="6">
                    <TextBlock Text="To Date" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,3"/>
                    <DatePicker SelectedDate="{Binding ToDate}" Height="35"/>
                </StackPanel>

                <Button Grid.Column="8"
                        Content="🔄 Refresh"
                        Command="{Binding RefreshCommand}"
                        Style="{StaticResource SecondaryButton}"
                        Height="35" VerticalAlignment="Bottom"/>

                <Button Grid.Column="10"
                        Content="✖ Clear"
                        Command="{Binding ClearFiltersCommand}"
                        Background="#757575" Foreground="White"
                        Height="35" Padding="12,8" Cursor="Hand"
                        FontWeight="SemiBold" BorderThickness="0"
                        VerticalAlignment="Bottom"/>
            </Grid>
        </Border>

        <!-- Results info + Grid -->
        <Border Grid.Row="2" Background="White" BorderBrush="#E0E0E0"
                BorderThickness="1" CornerRadius="6" Padding="15">
            <DockPanel>
                <!-- Record count bar -->
                <Border DockPanel.Dock="Top" Background="#F5F5F5" CornerRadius="4"
                        Padding="10,6" Margin="0,0,0,10" HorizontalAlignment="Left">
                    <TextBlock FontSize="12" Foreground="#555">
                        <Run Text="Showing"/>
                        <Run Text="{Binding TotalShown, Mode=OneWay}" FontWeight="Bold"/>
                        <Run Text="of"/>
                        <Run Text="{Binding TotalRecords, Mode=OneWay}" FontWeight="Bold"/>
                        <Run Text="records"/>
                    </TextBlock>
                </Border>

                <DataGrid ItemsSource="{Binding FilteredEntries}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          AlternatingRowBackground="#FAFAFA"
                          IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Timestamp"
                                            Binding="{Binding Timestamp, StringFormat='MM/dd/yyyy hh:mm:ss tt'}"
                                            Width="160"/>
                        <DataGridTemplateColumn Header="Action" Width="140">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="3" Padding="5,2" Margin="2" HorizontalAlignment="Left">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#F5F5F5"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Action}" Value="Sale">
                                                        <Setter Property="Background" Value="#E8F5E9"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Action}" Value="Product Added">
                                                        <Setter Property="Background" Value="#E3F2FD"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Action}" Value="Product Deleted">
                                                        <Setter Property="Background" Value="#FFEBEE"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Action}" Value="Product Loss">
                                                        <Setter Property="Background" Value="#FFEBEE"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Action}" Value="Restock">
                                                        <Setter Property="Background" Value="#E8F5E9"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Action}" Value="Manual Backup">
                                                        <Setter Property="Background" Value="#F3E5F5"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Action}" Value="Auto Backup">
                                                        <Setter Property="Background" Value="#F3E5F5"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding Action}" FontSize="11" FontWeight="SemiBold"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Entity" Binding="{Binding Entity}" Width="80"/>
                        <DataGridTextColumn Header="Name" Binding="{Binding EntityName}" Width="*"/>
                        <DataGridTextColumn Header="Details" Binding="{Binding Details}" Width="220"/>
                        <DataGridTextColumn Header="Old Value" Binding="{Binding OldValue}" Width="160"/>
                        <DataGridTextColumn Header="New Value" Binding="{Binding NewValue}" Width="160"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
