<UserControl x:Class="InventoryAndSalesSystem.Views.ProductListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InventoryAndSalesSystem.Views"
             mc:Ignorable="d" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Product List" FontSize="22" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBlock Text="View all products and their current stock levels" 
                      FontSize="12" Foreground="Gray"/>
        </StackPanel>

        <!-- Search and Filter Section -->
        <Border Grid.Row="1" Background="White" BorderBrush="#E0E0E0" 
               BorderThickness="1" CornerRadius="5" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="250"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Search Box -->
                <StackPanel Grid.Column="0">
                    <TextBlock Text="🔍 Search" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                            Height="35" Padding="8"
                            VerticalContentAlignment="Center"
                            FontSize="13">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="">
                                        <Setter Property="Background">
                                            <Setter.Value>
                                                <VisualBrush Stretch="None" AlignmentX="Left">
                                                    <VisualBrush.Visual>
                                                        <TextBlock Text="Search by name, category, or ID..." 
                                                                  Foreground="#999999" 
                                                                  Margin="10,0,0,0"
                                                                  FontSize="13"
                                                                  VerticalAlignment="Center"/>
                                                    </VisualBrush.Visual>
                                                </VisualBrush>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                </StackPanel>

                <!-- Category Filter -->
                <StackPanel Grid.Column="2">
                    <TextBlock Text="📁 Category" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <ComboBox ItemsSource="{Binding Categories}"
                             SelectedItem="{Binding SelectedCategoryFilter}"
                             Height="35" Padding="8"
                             VerticalContentAlignment="Center"
                             FontSize="13"/>
                </StackPanel>

                <!-- Clear Button -->
                <StackPanel Grid.Column="4" VerticalAlignment="Bottom">
                    <Button Content="✖ Clear Filters" 
                           Command="{Binding ClearSearchCommand}"
                           Background="#757575"
                           Foreground="White"
                           Height="35" 
                           Padding="15,8"
                           FontSize="13"
                           Cursor="Hand"
                           FontWeight="SemiBold"
                           BorderThickness="0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Results Count -->
        <Border Grid.Row="2" Background="#F5F5F5" 
               BorderBrush="#E0E0E0" BorderThickness="1" 
               CornerRadius="5" Padding="10" 
               Margin="0,0,0,10"
               VerticalAlignment="Top"
               HorizontalAlignment="Left">
            <TextBlock FontSize="12" Foreground="#555">
                <Run Text="Showing"/>
                <Run Text="{Binding FilteredProducts.Count, Mode=OneWay}" FontWeight="Bold"/>
                <Run Text="of"/>
                <Run Text="{Binding Products.Count, Mode=OneWay}" FontWeight="Bold"/>
                <Run Text="products"/>
            </TextBlock>
        </Border>

        <!-- Product Grid -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding FilteredProducts}"
                  SelectedItem="{Binding SelectedProduct}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  AlternatingRowBackground="#F5F5F5"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column" 
                  Margin="0,40,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" IsReadOnly="True"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Unit Cost" Binding="{Binding UnitCost, StringFormat=C, ConverterCulture='en-PH'}" Width="100" IsReadOnly="True"/>
                <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat=C, ConverterCulture='en-PH'}" Width="100" IsReadOnly="True"/>
                <DataGridTextColumn Header="Stock" Binding="{Binding Stock}" Width="80" IsReadOnly="True"/>
                <DataGridTextColumn Header="Min Stock" Binding="{Binding MinStock}" Width="80" IsReadOnly="True"/>
                <DataGridTemplateColumn Header="Status" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="3" Padding="5,2" HorizontalAlignment="Center">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsLowStock}" Value="True">
                                                <Setter Property="Background" Value="#ffebee"/>
                                                <Setter Property="BorderBrush" Value="#f44336"/>
                                                <Setter Property="BorderThickness" Value="1"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsLowStock}" Value="False">
                                                <Setter Property="Background" Value="#e8f5e9"/>
                                                <Setter Property="BorderBrush" Value="#4CAF50"/>
                                                <Setter Property="BorderThickness" Value="1"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="{Binding IsLowStock, Converter={StaticResource BoolToStatusConverter}}" 
                                          FontSize="11" FontWeight="SemiBold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsLowStock}" Value="True">
                                                    <Setter Property="Foreground" Value="#f44336"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsLowStock}" Value="False">
                                                    <Setter Property="Foreground" Value="#4CAF50"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
