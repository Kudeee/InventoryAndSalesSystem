<Window x:Class="InventoryAndSalesSystem.SalesReportWindow"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        xmlns:local="clr-namespace:InventoryAndSalesSystem"
        Title="Sales Report" Height="600" Width="900"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Sales Report" FontSize="24" 
                   FontWeight="Bold" Margin="0,0,0,20"/>

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Column="0" Background="#4CAF50" Padding="15" Margin="5">
                <StackPanel>
                    <TextBlock Text="Total Revenue" Foreground="White" FontSize="12"/>
                    <TextBlock Name="TotalRevenueText" Text="₱0.00" Foreground="White" 
                               FontSize="20" FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="#2196F3" Padding="15" Margin="5">
                <StackPanel>
                    <TextBlock Text="Total Products" Foreground="White" FontSize="12"/>
                    <TextBlock Name="TotalProductsText" Text="0" Foreground="White" 
                               FontSize="20" FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="#FF9800" Padding="15" Margin="5">
                <StackPanel>
                    <TextBlock Text="Total Stock" Foreground="White" FontSize="12"/>
                    <TextBlock Name="TotalStockText" Text="0" Foreground="White" 
                               FontSize="20" FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="3" Background="#9C27B0" Padding="15" Margin="5">
                <StackPanel>
                    <TextBlock Text="Total Sold" Foreground="White" FontSize="12"/>
                    <TextBlock Name="TotalSoldText" Text="0" Foreground="White" 
                               FontSize="20" FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Grid.ColumnSpan="4" Background="#607D8B" 
                    Padding="15" Margin="5,10,5,5">
                <StackPanel>
                    <TextBlock Text="Top Selling Product" Foreground="White" FontSize="12"/>
                    <TextBlock Name="TopProductText" Text="N/A" Foreground="White" 
                               FontSize="16" FontWeight="Bold"/>
                </StackPanel>
            </Border>
        </Grid>

        <lvc:CartesianChart Grid.Row="2" Series="{Binding SeriesCollection}">
            <lvc:CartesianChart.AxisX>
                <lvc:Axis Title="Products" Labels="{Binding Labels}">
                    <lvc:Axis.Separator>
                        <lvc:Separator Step="1"/>
                    </lvc:Axis.Separator>
                </lvc:Axis>
            </lvc:CartesianChart.AxisX>
            <lvc:CartesianChart.AxisY>
                <lvc:Axis Title="Revenue ($)" LabelFormatter="{Binding Formatter}"/>
            </lvc:CartesianChart.AxisY>
        </lvc:CartesianChart>
    </Grid>

</Window>
